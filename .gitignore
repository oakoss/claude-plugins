# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Node
node_modules/
npm-debug.log
pnpm-debug.log

# Local test artifacts
.tmp/

# review-cycle per-project state (this repo opts out of its own gate)
.claude/review-cycle/
.claude/.no-review-gate
# pre-0.16 layout, kept while sessions running the old plugin version remain
.claude/.review-mark
.claude/.review-in-progress
.claude/.review-stop-block

# Local Claude Code overrides
.claude/settings.local.json
# Session plan artifacts. A local choice, not one the plugin's init imposes —
# some projects commit their plans.
.claude/plans/

# Beads / Dolt
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
