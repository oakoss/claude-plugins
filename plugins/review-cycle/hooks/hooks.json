{
  "description": "review-cycle: refuses a commit unless a reviewer saw exactly what it records and the user asked for it; refuses a push the user did not ask for; flags comment slop in files the agent writes.",
  "modules": ["./register.ts"]
}
